{
  "name": "AIautoTestCase",
  "private": true,
  "workspaces": [
    "front",
    "server",
    "test-case-ai-mvp"
  ],
  "devDependencies": {
    "turbo": "^1.13.4"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test"
  }
}
